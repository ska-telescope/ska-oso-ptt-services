apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: zookeeper-data
  name: zookeeper-data
  namespace: ska-oso-ptt-services
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
